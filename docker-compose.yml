version: "3"

services:
  nuxt:
    build: ./
    container_name: nuxt-website
    restart: always
    ports:
      - "3000:3000"
    command:
      "npm run start"

  nginx:
    image: nginx:1.15
    container_name: nuxt-nginx
    ports:
      - "80:80"    
    volumes:
      - ./nginx:/etc/nginx/conf.d
    depends_on:
      - nuxt